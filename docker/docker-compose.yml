version: '3.8'
services:
  api:
    build: .
    ports:
      - "4000:4000"
    depends_on:
      - postgres
      - redis
  postgres:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: postgres
  redis:
    image: redis:7
